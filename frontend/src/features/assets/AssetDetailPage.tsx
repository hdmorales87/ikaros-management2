import { FormEvent, useState } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { useParams } from 'react-router-dom'
import { assetApi, getGridRows } from '../../api'
import AttachmentPanel from '../files/AttachmentPanel'
import TechnicalSheetPanel from '../technical-sheets/TechnicalSheetPanel'

type Form = { nombre: string; codigo: string; marca: string; tipo: string; departamento: string; proveedor: string; estado: string; asignado: string; precio: string; compra: string; factura: string; ubicacion: string }
const emptyForm: Form = { nombre: '', codigo: '', marca: '', tipo: '', departamento: '', proveedor: '', estado: '', asignado: '', precio: '', compra: '', factura: '', ubicacion: '' }

export default function AssetDetailPage() {
  const { id = '0' } = useParams(); const queryClient = useQueryClient(); const [form, setForm] = useState<Form>(emptyForm); const [loadedId, setLoadedId] = useState(''); const [message, setMessage] = useState('')
  const asset = useQuery({ queryKey: ['v1-asset', id], queryFn: () => assetApi.find(Number(id)) })
  const types = useQuery({ queryKey: ['asset-types'], queryFn: () => getGridRows('activos_tipos', '', {}, ['nombre']) }); const departments = useQuery({ queryKey: ['asset-departments'], queryFn: () => getGridRows('departamentos', '', {}, ['nombre']) }); const providers = useQuery({ queryKey: ['asset-providers'], queryFn: () => getGridRows('terceros', '', {}, ['razon_social']) }); const states = useQuery({ queryKey: ['asset-states'], queryFn: () => getGridRows('activos_estados', '', {}, ['nombre']) }); const users = useQuery({ queryKey: ['asset-users'], queryFn: () => getGridRows('users', '', { activo: true }, ['nombre', 'apellido']) })
  const row = asset.data
  if (row && loadedId !== id) { setLoadedId(id); setForm({ nombre: String(row.nombre ?? ''), codigo: String(row.codigo ?? ''), marca: String(row.marca ?? ''), tipo: String(row.id_tipo ?? ''), departamento: String(row.id_departamento ?? ''), proveedor: String(row.id_proveedor ?? ''), estado: String(row.estado ?? ''), asignado: String(row.id_asignado ?? ''), precio: String(row.precio_compra ?? ''), compra: String(row.fecha_compra ?? '').slice(0, 10), factura: String(row.numero_factura ?? ''), ubicacion: String(row.id_ubicacion ?? '') }) }
  const save = useMutation({ mutationFn: () => assetApi.update(Number(id), { nombre: form.nombre, codigo: form.codigo || null, marca: form.marca || null, id_tipo: form.tipo ? Number(form.tipo) : null, id_departamento: form.departamento ? Number(form.departamento) : null, id_proveedor: Number(form.proveedor), estado: form.estado ? Number(form.estado) : null, id_asignado: form.asignado ? Number(form.asignado) : null, precio_compra: form.precio ? Number(form.precio) : null, fecha_compra: form.compra || null, numero_factura: form.factura || null, id_ubicacion: form.ubicacion ? Number(form.ubicacion) : null }), onSuccess: () => { setMessage('Activo actualizado.'); queryClient.invalidateQueries({ queryKey: ['v1-asset', id] }); queryClient.invalidateQueries({ queryKey: ['v1-assets'] }) }, onError: () => setMessage('No fue posible actualizar el activo.') })
  function field(key: keyof Form, value: string) { setForm((current) => ({ ...current, [key]: value })) }
  function submit(event: FormEvent) { event.preventDefault(); save.mutate() }
  if (!row) return <section className="panel"><p className="muted">{asset.isLoading ? 'Cargando activo...' : 'Activo no encontrado.'}</p></section>
  return <section><div className="topbar"><div><h1>Activo #{id}</h1><p className="muted">Detalle, asignación, compra y ficha técnica.</p></div></div><div className="panel"><form onSubmit={submit}><div className="form-grid"><label className="field">Nombre<input value={form.nombre} onChange={(event) => field('nombre', event.target.value)} required /></label><label className="field">Código<input value={form.codigo} onChange={(event) => field('codigo', event.target.value)} /></label><label className="field">Marca<input value={form.marca} onChange={(event) => field('marca', event.target.value)} /></label><label className="field">Tipo<select value={form.tipo} onChange={(event) => field('tipo', event.target.value)}><option value="">Sin tipo</option>{types.data?.map((item) => <option value={String(item.id)} key={String(item.id)}>{String(item.nombre)}</option>)}</select></label><label className="field">Departamento<select value={form.departamento} onChange={(event) => field('departamento', event.target.value)}><option value="">Sin departamento</option>{departments.data?.map((item) => <option value={String(item.id)} key={String(item.id)}>{String(item.nombre)}</option>)}</select></label><label className="field">Proveedor<select value={form.proveedor} onChange={(event) => field('proveedor', event.target.value)} required><option value="">Selecciona proveedor</option>{providers.data?.map((item) => <option value={String(item.id)} key={String(item.id)}>{String(item.razon_social ?? item.nombre)}</option>)}</select></label><label className="field">Estado<select value={form.estado} onChange={(event) => field('estado', event.target.value)}><option value="">Sin estado</option>{states.data?.map((item) => <option value={String(item.id)} key={String(item.id)}>{String(item.nombre)}</option>)}</select></label><label className="field">Asignado a<select value={form.asignado} onChange={(event) => field('asignado', event.target.value)}><option value="">Sin asignación</option>{users.data?.map((item) => <option value={String(item.id)} key={String(item.id)}>{String(item.nombre)} {String(item.apellido)}</option>)}</select></label><label className="field">Precio de compra<input type="number" min="0" value={form.precio} onChange={(event) => field('precio', event.target.value)} /></label><label className="field">Fecha de compra<input type="date" value={form.compra} onChange={(event) => field('compra', event.target.value)} /></label><label className="field">Factura<input value={form.factura} onChange={(event) => field('factura', event.target.value)} /></label><label className="field">Ubicación (ID)<input type="number" min="1" value={form.ubicacion} onChange={(event) => field('ubicacion', event.target.value)} /></label></div>{message && <p className="muted" role="status">{message}</p>}<button className="primary" disabled={save.isPending}>Guardar activo</button></form></div><TechnicalSheetPanel tabla="activos" idMaestro={Number(id)} /><AttachmentPanel table="activos" id={Number(id)} /></section>
}